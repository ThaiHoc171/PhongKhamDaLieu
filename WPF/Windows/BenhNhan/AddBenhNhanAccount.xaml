<Window x:Class="WPF.Windows.BenhNhan.AddBenhNhanAccount"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        ResizeMode="NoResize" Width="400" Height="340" 
        WindowStartupLocation="CenterScreen" WindowStyle="None">
    <Border BorderBrush="#8F8FAF" BorderThickness="1">
        <Grid Background="AliceBlue">

            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <!-- ================= HEADER ================= -->
            <Border Grid.Row="0"
                Background="{StaticResource PrimaryBrush}"
                Padding="12,6"
                MouseDown="Header_MouseDown">

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="AccountPlus" Width="50" Height="50"
                                             Margin="0,0,10,0" Foreground="White"/>

                    <TextBlock Text="THÊM TÀI KHOẢN BỆNH NHÂN"
                               FontSize="20" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- ================= CONTENT ================= -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <!-- Tên -->
                <TextBox x:Name="txtName" Grid.Row="0" Height="40" Padding="12,6"
                         materialDesign:HintAssist.Hint="Họ và tên"/>
                <!-- Email -->
                <TextBox x:Name="txtEmail" Grid.Row="1" Height="40" Padding="12,6"
                         materialDesign:HintAssist.Hint="Email" />

                <!-- Password -->
                <PasswordBox x:Name="txtPassword" Grid.Row="2" Height="40" Padding="12,6"
                             materialDesign:HintAssist.Hint="Mật khẩu"/>

            </Grid>

            <!-- ================= FOOTER ================= -->
            <Border Grid.Row="2" Background="#E5E9F8" Padding="5">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                    <Button x:Name="btnClose" Click="btnClose_Click" Width="100" Margin="5">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Close"/>
                            <TextBlock Text="Đóng" Margin="5,0"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="btnSave" Click="btnSave_Click" Width="100" Margin="5" 
                            Style="{StaticResource PrimaryButton}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ContentSave"/>
                            <TextBlock Text="Lưu" Margin="5,0"/>
                        </StackPanel>
                    </Button>

                </StackPanel>
            </Border>

        </Grid>
    </Border>
</Window>
